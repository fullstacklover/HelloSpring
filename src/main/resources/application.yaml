spring:
  application:
    name: HelloSpring

  datasource:
    url: jdbc:mysql://localhost:3306/isi_hello_spring
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
